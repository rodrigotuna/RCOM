#ifndef _UTILS_H_
#define _UTILS_H_

int destuff_frame(char * destuff_buf, char * buf, int sz);
int stuff_frame(char * stuf_buf, char * buf, int sz);
char bcc_buf(char * buf, int sz);

#endif // _UTILS_H